[package]
name = "iced-wordclock"
version = "0.1.0"
edition = "2021"

[dependencies]
wordclock = { path = "../" }
iced = { version = "0.9" , features = ["tokio", "debug"] }
time = { version = "0.3.5", features = ["local-offset"] }
